{
  "name": "double-trigger",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ eva/double-trigger",
    "create-local": "graph create --node http://localhost:8020/ eva/double-trigger",
    "remove-local": "graph remove --node http://localhost:8020/ eva/double-trigger",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 eva/double-trigger",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0-alpha.0",
    "@graphprotocol/graph-ts": "0.28.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
